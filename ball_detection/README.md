# 小球偵測、位置判斷

## 環境安裝

使用 `pip` 安裝

```bash
$ pip install -r requirements.txt
```

使用 `poetry` 安裝

```bash
$ poetry install --no-root
```

## 環境設定

依照 `.env.example` 建立 `.env`

-   HFOV 值可以由使用相機本身規格取得，亦可透過執行 `hfov.py` 協助計算取得

## 執行方式

```bash
$ python main.py
```
